module.exports = 
{
	BMart_addProductInfo_sanity(addr, jobObj) 
	{
		return true;
	},

	BMart_delistProduct_sanity(addr, jobObj) 
	{
		return true;
	},

	BMart_changePrice_sanity(addr, jobObj) 
	{
		return true;
	},

	BMart_withdraw_sanity(addr, jobObj) 
	{
		return true;
	}
}
