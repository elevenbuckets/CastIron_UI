wallet.allAccounts() {return [,]}
wallet.ethNetStatus() {return {blockHeight: , blockTime }}
wallet.gasPriceEst() {return {type:price, ... }}

wallet.setAccount(addr) { return boolean }
wallet.enqueueTx(token)(addr, amount, gas){ return jobObj} 

wallet.processJobs([] jobList){return Q}


wallet.rcdQ[Q] = [{txHash:, ..}, ...]
wallet.getReceipt([txHash,...]) { return promise}